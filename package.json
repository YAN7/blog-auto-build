{
  "name": "auto-build",
  "version": "1.0.0",
  "description": "github webhook api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start index.js --name auto-build"
  },
  "author": "YAN7",
  "license": "ISC",
  "dependencies": {
    "github-webhook-handler": "^0.7.1"
  },
  "devDependencies": {
    "nodemon": "^1.18.11"
  }
}
